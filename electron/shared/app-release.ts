/** 与 electron-builder artifactName 一致：NioZy-${version}-${arch}.exe */
export const INSTALLER_BASENAME = (version: string) => `NioZy-${version}-x64.exe`

/** GitHub Releases 页面（与 src/constants/urls.ts 保持同源） */
export const GITHUB_RELEASES_URL = 'https://github.com/Landers1037/NioZy/releases'
