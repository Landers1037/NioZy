# 功能：桌面宠物

在桌面显示透明悬浮宠物窗口，播放 Petdex/Codex 精灵图动画；支持导入与管理宠物、切换动画状态，并与提醒事项联动（查看列表、到点弹窗）。

详细设置入口：**设置 → 提醒**（与提醒事项共用 `reminder` 配置段）。完整说明见 [功能提醒事项.md](./功能提醒事项.md) 中的桌面宠物相关小节。

## 功能列表

- **开启桌面宠物**（`reminder.desktopPetEnabled`）
- **宠物大小**（`reminder.desktopPetScale`）：显示缩放 25%–200%，默认 50%；设置页滑块拖动预览、松手保存
- **导入精灵图**：Petdex 格式 WebP（1536×1872，8×9 网格，单帧 192×208），保存至 `pets/<名称>/spritesheet.webp`
- **宠物管理**：切换当前宠物、删除宠物（含目录与精灵图）
- **动画状态**：按精灵图行解析 9 种默认状态（`idle` 等），可选手动选择；可选 `pet.json` 覆盖 `states`
- **随机状态**：开启后每 10s 随机切换一行动画（`PET_RANDOM_STATE_INTERVAL_MS`）
- **交互**：左键拖动定位；双击或右键菜单切换主窗口显示/隐藏（与全局快捷键 Ctrl+T 相同逻辑）
- **右键菜单**：显示/隐藏 NioZy、新建终端、查看提醒、关闭桌面宠物
- **查看提醒**：在宠物渲染页展开提醒列表（未关闭项，按时间排序）
- **到点提醒**：提醒功能开启且桌面宠物开启时，在宠物右上方弹出对话框（推迟/关闭），与主窗口 Toast/弹框并行

## 进程归属

| 层级 | 文件 |
|------|------|
| **主进程** | `electron/desktop-pet-service.ts`（窗口、菜单、拖拽、尺寸） |
| **主进程** | `electron/pet-store.ts`（导入/删除/列表/精灵配置） |
| **共享** | `electron/shared/pet-atlas.ts`、`pet-animation-states.ts`、`pet-animation-states-resolve.ts` |
| **共享** | `electron/shared/pet-window-layout.ts`、`pet-ui-labels.ts`、`pet-reminder-dto.ts` |
| **Preload** | `electron/preload/pet-preload.ts` → `window.petAPI` |
| **宠物渲染页** | `src/pet/index.html`、`main.ts`、`pet.css`、`pet-reminder-ui.ts` |
| **主窗口设置** | `src/components/settings/ReminderSettings.tsx`、`PetSpritePreview.tsx` |
| **全局快捷键复用** | `electron/global-shortcuts.ts` → `toggleMainWindowForeground` |

构建：`electron.vite.config.ts` 额外入口 `src/pet/index.html`、`electron/preload/pet-preload.ts`。

## 架构与数据流

### 宠物窗口生命周期

```mermaid
flowchart TB
  Settings["settings.json reminder.*"]
  Sync["syncDesktopPet()"]
  Svc["desktop-pet-service"]
  Win["BrowserWindow 透明置顶"]
  PetUI["src/pet 渲染页"]

  Settings -->|desktopPetEnabled| Sync
  Sync --> Svc
  Svc -->|show/hide/reload| Win
  Win --> PetUI
  PetUI -->|pet:getSpriteConfig| Store["pet-store"]
```

```mermaid
sequenceDiagram
  participant Set as ReminderSettings
  participant Main as main/index.ts
  participant Svc as desktop-pet-service
  participant Pet as src/pet

  Set->>Main: settings:save reminder
  Main->>Svc: syncDesktopPet()
  Pet->>Main: pet:getSpriteConfig
  Main->>Pet: spriteUrl + states + animationStateId + displayScale
  Pet->>Pet: 按 displayScale 设尺寸 / 帧动画 / 随机切换
  Pet->>Main: pet:ready
```

### 交互与右键菜单

```mermaid
sequenceDiagram
  participant User as 用户
  participant Pet as 宠物渲染页
  participant Svc as desktop-pet-service
  participant Main as 主窗口

  User->>Pet: pointerdown/move/up
  Pet->>Svc: pet:pointer* → setBounds
  User->>Pet: dblclick / contextmenu
  Pet->>Svc: pet:toggleMain / pet:showMenu
  Svc->>Main: toggleMainWindowForeground
  User->>Svc: 菜单「新建终端」
  Svc->>Main: requestNewTerminal → app:newTerminal
  User->>Svc: 菜单「查看提醒」
  Svc->>Pet: pet:openReminders
```

### 提醒联动

```mermaid
sequenceDiagram
  participant Sched as reminder-scheduler
  participant Main as main/index.ts
  participant Pet as src/pet

  Sched->>Main: reminder:due
  Main->>Main: sendToRenderer 主窗口
  Main->>Pet: notifyPetReminderDue → pet:reminderDue
  Pet->>Pet: 右上方 due 对话框
  Pet->>Main: pet:snoozeReminders / pet:dismissReminders
```

## 实验特性

否（入口在 **设置 → 提醒**）。

## 配置文件片段

`settings.json` → `reminder`（宠物相关字段）：

```json
{
  "reminder": {
    "desktopPetEnabled": false,
    "desktopPetId": null,
    "desktopPetAnimationState": "idle",
    "desktopPetRandomState": false,
    "desktopPetPosition": { "x": 1200, "y": 800 },
    "desktopPetScale": 0.5
  }
}
```

类型定义：`electron/shared/reminder-settings.ts`。

## 数据存储

| 路径 | 内容 |
|------|------|
| `%USERPROFILE%\.config\NioZy\pets\<id>\spritesheet.webp` | 宠物精灵图 |
| `%USERPROFILE%\.config\NioZy\pets\<id>\pet.json` | 可选，自定义 `states` 动画配置 |

```83:95:electron/config-paths.ts
export function getPetsDir(): string {
  return join(getConfigDir(), 'pets')
}
export function getPetDir(petId: string): string {
  return join(getPetsDir(), petId)
}
export function getPetSpritesheetPath(petId: string): string {
  return join(getPetDir(petId), 'spritesheet.webp')
}
```

## 精灵图规格

```1:40:electron/shared/pet-atlas.ts
/** Petdex / Codex 精灵图规格：1536×1872，8×9 网格，单帧 192×208 */
export const PET_ATLAS = { /* cellWidth: 192, cellHeight: 208, ... */ }

export const PET_DISPLAY_SCALE_MIN = 0.25   // 25%
export const PET_DISPLAY_SCALE_MAX = 2      // 200%
export const PET_DISPLAY_SCALE_STEP = 0.01  // 滑块步进 1%
export const PET_DISPLAY_SCALE_DEFAULT = 0.5

export function normalizePetDisplayScale(stored: number | undefined): number
export function getPetDisplayDimensions(scale: number): { width: number; height: number }
```

桌面显示尺寸 = 单帧 192×208 × `desktopPetScale`（经 `getPetDisplayDimensions` 取整）。默认 50% 时为 **96×104 px**。

## 默认动画状态（9 行）

定义于 `electron/shared/pet-animation-states.ts`：`idle`、`running-right`、`running-left`、`waving`、`jumping`、`failed`、`waiting`、`running`、`review`。每状态含 `row`、`frames`、`durationsMs[]`。

读取 `pet.json` 的 Node 逻辑在 `electron/shared/pet-animation-states-resolve.ts`（仅主进程，避免渲染层引用 `fs`）。

## 窗口尺寸模式

`electron/shared/pet-window-layout.ts` 按 `desktopPetScale` 动态计算（`getPetWindowCompact` 等）：

| 模式 | 尺寸 | 触发 |
|------|------|------|
| compact | 宠物宽×高 | 默认 / 关闭面板 |
| reminder list | 300 × (宠物高 + 220) | 查看提醒 |
| due alert | max(宠物宽, 260) × (宠物高 + 148) | 到点弹窗 |
| both | 300 × (宠物高 + 220 + 148) | 列表 + 到点同时显示 |

默认 50% 缩放下 compact 约为 96×104，reminder list 约为 300×324。缩放变更后 `syncDesktopPet()` 会重设 compact 尺寸并 reload 渲染页。

布局 CSS：`src/pet/pet.css`（`.pet-stage` 底部对齐，`.pet-reminder-panel` 紧贴宠物上方）；`.pet` 宽高由 `src/pet/main.ts` 按 `displayScale` 内联设置。

## IPC

### 主窗口（`reminder` 命名空间）

| 通道 | 说明 |
|------|------|
| `reminder:listPets` | 已导入宠物 id 列表 |
| `reminder:importPet` | 选择 WebP 并导入 |
| `reminder:deletePet` | 删除宠物目录 |
| `reminder:listPetAnimationStates` | 某宠物的动画状态列表 |
| `reminder:getPetPreviewUrl` | 设置页预览 URL |

### 宠物窗口（`pet:*`）

| 通道 | 说明 |
|------|------|
| `pet:ready` / `pet:pointerDown` / `Move` / `Up` | 就绪与拖拽 |
| `pet:toggleMain` / `pet:showMenu` | 双击与右键菜单 |
| `pet:getSpriteConfig` | 精灵图 URL、状态、随机开关、`displayScale` |
| `pet:getLabels` / `pet:listReminders` | 多语言文案与提醒列表 |
| `pet:dismissReminders` / `pet:snoozeReminders` | 宠物到点弹窗操作 |
| `pet:setWindowCompact` / `ReminderList` / `DueAlert` / `ReminderAndDue` | 窗口尺寸 |
| `pet:openReminders`（主→渲染事件） | 打开提醒列表 |
| `pet:reminderDue`（主→渲染事件） | 到点 payload |

Preload：`electron/preload/pet-preload.ts` 暴露 `window.petAPI`。

## 核心代码

### desktop-pet-service

- `createPetWindow()`：透明、`alwaysOnTop: pop-up-menu`、`type: toolbar`（Windows）；初始尺寸取自 `getPetDisplayDimensions(desktopPetScale)`
- `getPetScale()` / `getPetDisplaySize()`：读取当前 `desktopPetScale` 并换算像素尺寸
- `popupPetContextMenu()`：菜单项与 `openPetReminders()`、`requestNewTerminal()`
- `notifyPetReminderDue()`：条件 `reminder.enabled && desktopPetEnabled`
- `saveDesktopPetPosition()`：拖拽结束后写入 `desktopPetPosition`；拖拽锚点为宠物精灵左下角，随缩放变化

### pet-store

- `pickAndImportPet()`：对话框选 WebP，复制到 `pets/<id>/`
- `getDesktopPetSpriteConfig()`：占位模式或 sprite 模式配置（含 `displayScale`）
- `deletePet()` / `listPetAnimationStates()`

### 宠物渲染页

- `src/pet/main.ts`：`applyDisplayScale()` 设元素与精灵图尺寸；精灵帧动画、`setTimeout` 按 `durationsMs` 切帧
- `ReminderSettings.tsx`：宠物大小滑块（`draftPetScale` 拖动预览，`onValueCommit` 写入 `desktopPetScale`）
- `src/pet/pet-reminder-ui.ts`：提醒列表与到点对话框 DOM

## 注意事项

- 透明窗口使用极低 alpha 背景（`rgba(255,255,255,0)` + 命中层）避免 Windows 鼠标穿透。
- `pet-animation-states.ts` 不得直接 `import fs`；文件读取仅在 `pet-animation-states-resolve.ts`。
- 到点弹窗在宠物侧独立展示，不替代主窗口 `notifyMode`（toast/dialog）行为。
- `normalizePetDisplayScale` 以 `PET_DISPLAY_SCALE_MIN` 为基准按步进对齐，避免 25% 被舍入到 30%；设置页滑块仅在松手时保存，避免拖动中频繁 reload 宠物窗口。
