# 功能：实验特性

设置中心「实验特性」分区内的开关及关联子系统。

## 功能列表总览

| 子功能 | 配置键 | 默认 |
|--------|--------|------|
| 终端模拟器 | `experimental.terminalEmulator` | `xterm`（可选 `ghostty`、`wterm`） |
| Ghostty WASM 核心 | `experimental.ghosttyCoreEnabled` | `false` |
| Ghostty 回滚上限 | `experimental.ghosttyScrollbackLimit` | `10000` |
| VNC Web Viewer | `experimental.vncWebEnabled` | `false` |
| VNC 缩放/硬件加速/光标/编码 | `vncAdaptiveScale` 等 | 见类型 |
| Attach-PTY 渲染 | `experimental.attachPtyRenderMode` | `false` |
| Attach Tab 停留时间 | `experimental.attachPtyTabSwitchDwellMs` | `300` |
| Attach WebGL 上下文池 | `experimental.attachPtyWebglContextPool` | `false` |
| Attach Scrollback 卸载 | `experimental.attachPtyScrollbackOffload` | `false` |
| AI 边栏 | `experimental.aiSidebarEnabled` | `false` |
| AI 附件 | `experimental.aiAttachmentsEnabled` | `false` |
| JS 沙箱 Tab | `experimental.jsSandboxEnabled` | `false` |

独立文档：[功能AI助手边栏.md](./功能AI助手边栏.md)、[功能终端与会话.md](./功能终端与会话.md)（Wterm/Attach）、[功能性能.md](./功能性能.md)（终端输出缓冲与 CPU 优化）、[功能增强SHELL.md](./功能增强SHELL.md)（内联图片 / `niozy-cat`）。

## 架构与数据流

### 实验开关与模块映射

```mermaid
flowchart TB
  Exp["settings.experimental"]
  Exp --> Xterm["terminalEmulator=xterm → TerminalView + xterm.js"]
  Exp --> GhosttyWeb["terminalEmulator=ghostty → TerminalView + ghostty-web"]
  Exp --> Wterm["terminalEmulator=wterm → WterminalView"]
  Exp --> Ghostty["ghosttyCoreEnabled → Wterm WASM VT"]
  Exp --> Attach["attachPtyRenderMode → AttachPtyTerminalHost"]
  Exp --> VNC["vncWebEnabled → vnc-proxy + VncPanel"]
  Exp --> AI["aiSidebarEnabled → Copilot Runtime"]
  Exp --> JS["jsSandboxEnabled → JsSandboxPanel QuickJS"]
```

### Attach-PTY 切换数据流

```mermaid
sequenceDiagram
  participant User as 切换 Tab
  participant Hook as useAttachPtyTabSwitch
  participant Stream as useTerminalStreamSync
  participant Host as AttachPtyTerminalHost
  participant X as 单个 xterm 实例
  participant TS as TerminalService

  User->>Hook: activeTabId 变化
  Hook->>Host: 立即 commit(targetTerminalId)
  Stream->>TS: setActiveStreams（Attach 下仅 committed id）
  TS->>TS: 非活跃流 pause(PTY/SSH)；活跃流 resume + flush pausedOutput
  Host->>X: detach 旧会话（dwell 内可跳过快照）
  Host->>X: 清空 + 恢复快照/offload
  TS-->>X: terminal:data（仅 committed 终端）
```

**说明**：

- Tab 切换**立即** commit，不再等待 dwell；`attachPtyTabSwitchDwellMs` 仅用于 detach 时是否保存快照（停留不足则跳过，依赖主进程 `pausedOutput` 续流）。
- **Attach 模式推流**：`useTerminalStreamSync` 经 `resolveAttachPtyTargetTab` 同步推导目标 `terminalId`，仅该 id 进入 `activeStreamIds`；切到非终端 Tab 时传 `[]`。分屏 Tab 仍按活动 Tab 全部 pane 推流。
- **主进程反压**：`setActiveStreams` 对移出集合的会话 `inactive` pause；活跃流经 `TerminalActiveOutputGate` 推流，未 ack 超限时 `flow` pause；回到活跃集合时清除 `inactive` 并 flush `pausedOutput`。

## 进程归属

| 子功能 | 主进程 | 渲染层 |
|--------|--------|--------|
| Ghostty（ghostty-web） | `shell-integration.ts`（`NIOZY_IMAGE_PROTOCOL=kitty`） | `TerminalView` + `ghostty-web-loader.ts` |
| Wterm/Ghostty Core | — | `WterminalView`、`wterm-ghostty-core.ts` |
| VNC | `vnc-proxy.ts` | `VncPanel.tsx` |
| Attach-PTY | `terminal-service.ts`、`terminal-active-output-gate.ts`（推流 + 闭环反压） | `AttachPtyTerminalHost.tsx`、`terminal-write-batcher.ts`、`terminal-flow-control.ts` |
| AI Runtime | `copilot/runtime-server.ts` | AI 边栏 |
| JS 沙箱 | — | `JsSandboxPanel.tsx`、`js-sandbox-client.ts` |

## 实验特性

**全部为实验性** — 需用户在设置中显式开启。

## 配置文件片段

完整类型：`37:103:electron/shared/experimental-settings.ts`。

```json
{
  "experimental": {
    "terminalEmulator": "xterm",
    "ghosttyCoreEnabled": false,
    "ghosttyScrollbackLimit": 10000,
    "vncWebEnabled": false,
    "vncAdaptiveScale": true,
    "vncHardwareAccel": false,
    "vncLocalCursor": true,
    "vncEncoding": "tight",
    "attachPtyRenderMode": false,
    "attachPtyTabSwitchDwellMs": 300,
    "attachPtyWebglContextPool": false,
    "attachPtyScrollbackOffload": false,
    "aiSidebarEnabled": false,
    "aiAttachmentsEnabled": false,
    "aiSidebarWidth": "medium",
    "aiRuntimePort": 3006,
    "aiProvider": "openai",
    "aiModel": "gpt-4o-mini",
    "aiBaseUrl": "",
    "aiApiKey": "",
    "jsSandboxEnabled": false
  }
}
```

## 数据存储

仅存于 `settings.json` 的 `experimental` 对象；JS 沙箱无持久化脚本库。

## 核心代码

### 规范化与 DOM 渲染器约束

`experimental.terminalEmulator` 为 `wterm` 或 `ghostty` 时，`terminal.renderer` 强制为 `dom`（不支持 xterm WebGL）。

```190:197:electron/shared/experimental-settings.ts
export function normalizeRendererForWterm(
  emulator: TerminalEmulator,
  renderer: TerminalRenderer,
): TerminalRenderer {
  if (emulator === 'wterm' || emulator === 'ghostty') return WTERM_RENDERER
  return renderer
}
```

类型：`TerminalEmulator = 'xterm' | 'ghostty' | 'wterm'`（`electron/shared/experimental-settings.ts`）。

### 设置 UI

`src/components/settings/ExperimentalSettings.tsx`

### JS 沙箱 Tab

```42:46:src/App.tsx
const JsSandboxPanel = lazy(() =>
  import('@/components/sandbox/JsSandboxPanel').then(/* ... */),
)
```

`MinimalTabBar` — `jsSandboxEnabled` 时显示 Braces 按钮（`26:111:src/components/layout/MinimalTabBar.tsx`）。

实现：`src/components/sandbox/JsSandboxPanel.tsx`、`src/lib/js-sandbox-client.ts`（QuickJS WASM）。

### VNC

`experimental.vncWebEnabled` 为 true 时连接管理可添加 VNC Tab；`electron/vnc-proxy.ts` 提供 WebSocket 代理。

### Attach-PTY

**目标**：多 Tab 场景下共用一个 xterm 实例，降低内存与 WebGL 上下文数量；分屏 Tab 仍使用多个 `TerminalView`。

| 开关 | 作用 |
|------|------|
| `attachPtyRenderMode` | 单 Tab 无分屏时由 `AttachPtyTerminalHost` 承载唯一 xterm |
| `attachPtyTabSwitchDwellMs` | detach 快照 dwell（0–5000ms，默认 300） |
| `attachPtyWebglContextPool` | 单例宿主复用 WebGL 槽位，切换 Tab 不随 terminalId 释放上下文 |
| `attachPtyScrollbackOffload` | detach 时将 scrollback 卸到侧存储，减轻 xterm 内存 |

**生效条件**：`attachPtyRenderMode && terminalEmulator === 'xterm'`（Ghostty / Wterm 下开关禁用）。

**核心模块**：

- `src/stores/attach-pty-session-store.ts` — committed 会话与 Tab 快照
- `src/hooks/useAttachPtyTabSwitch.ts` — Tab 切换立即 commit
- `src/hooks/useTerminalStreamSync.ts` — 声明 `activeStreamIds`；Attach 下仅 committed 终端
- `src/lib/inactive-tab-memory.ts` — `collectActiveTerminalStreamIds`（含 Attach 分支）
- `src/lib/attach-pty-render.ts` — 模式判定、dwell、子开关
- `src/components/terminal/AttachPtyTerminalHost.tsx` — 全局单例 xterm 宿主
- `src/lib/attach-pty-scrollback-offload.ts` — scrollback 侧存储（`lru-cache`）
- `src/lib/attach-pty-webgl-pool.ts` — WebGL 槽位复用
- `electron/terminal-service.ts` — `setActiveStreams`、`pausedOutput`、inactive/flow 双原因 `pause`/`resume`、闭环反压 gate
- `electron/terminal-active-output-gate.ts` — 活跃推流闸门（未 ack 上限 → flow pause）
- `electron/shared/terminal-flow-limits.ts` — 反压水位线常量

#### Scrollback Offload 侧存储（`lru-cache`）

开启 `attachPtyScrollbackOffload` 时，detach 的 VT/纯文本快照写入 `attach-pty-scrollback-offload.ts`，**不进 zustand**，避免大字符串触发 React 重渲染。

| 策略 | 值 |
|------|-----|
| 最大条目数 | 50 |
| 总字节上限 | 64 MB（UTF-16 估算） |
| TTL | 30 分钟未访问自动过期 |
| 驱逐 | 超出条目数或总字节时 LRU 淘汰 |

API：`offload` / `peek` / `take` / `clear` / `reset`；调试可用 `getOffloadCacheStats()`。

依赖：`lru-cache`（`package.json`）。

**与一键降级联动**：`advanced.resourceAutoDegrade` 触发「一键降级」时会自动开启 `attachPtyRenderMode`（见 [功能性能.md](./功能性能.md)）。

#### 高频输出与 CPU（xterm 通用，含 Attach 宿主）

Attach-PTY 优化**多 Tab 内存**与**后台洪水不拖垮 UI**；**当前活跃 Tab** 则通过 **Tabby 同款闭环反压**（FlowControl + ack）在 `yes` 等极端输出下仍保留拖拽/输入响应。

##### 闭环反压（活跃 Tab，默认开启）

参考 Tabby `PTYDataQueue` + `FlowControl`，主进程与渲染层形成 ack 闭环：

```mermaid
flowchart LR
  PTY["PTY / SSH"] --> Gate["TerminalActiveOutputGate\n100KB/chunk\n500KB 未 ack"]
  Gate -->|"未 ack 超限"| FlowPause["pty.pause() flow"]
  Gate --> Flush["terminal-output-flush\n16ms + 256KB"]
  Flush --> IPC["terminal:data\n64KB 分片"]
  IPC --> Batcher["terminal-write-batcher"]
  Batcher --> FC["TerminalFlowControl\n≤10 pending write"]
  FC --> Xterm["xterm.write(callback)"]
  Xterm --> Ack["terminal:ackData(id, len)"]
  Ack --> Gate
  Ack --> FC
```

```mermaid
sequenceDiagram
  participant PTY as PTY / SSH
  participant Gate as ActiveOutputGate
  participant Main as 主进程 flusher
  participant R as 渲染层 batcher
  participant X as xterm
  participant FC as FlowControl

  PTY->>Gate: onData 推送
  Gate->>Main: onEmit chunk（unacked += len）
  alt unacked > 500KB
    Gate->>PTY: flow pause
  end
  Main->>R: terminal:data
  R->>FC: pumpLoop
  alt pendingCallbacks > 10
    FC-->>R: blocked，await
  end
  FC->>X: write(chunk, callback)
  X-->>R: callback
  R->>Main: terminal:ackData(id, len)
  Main->>Gate: ack(len)
  alt unacked ≤ 500KB
    Gate->>PTY: flow resume
  end
```

| 层级 | 文件 | 策略 |
|------|------|------|
| 推流集合 | `useTerminalStreamSync.ts`、`inactive-tab-memory.ts` | Attach 下仅 committed `terminalId`；非终端 Tab 为 `[]` |
| 非活跃反压 | `electron/terminal-service.ts` | 非活跃流 `inactive` pause；`pausedOutput` 上限 512KB |
| **活跃推流闸门** | `electron/terminal-active-output-gate.ts` | 单次 emit ≤100KB；未 ack ≤500KB；超限 `flow` pause PTY/SSH |
| 主进程合并 | `electron/main/terminal-output-flush.ts` | 16ms flush；拖拽时 `pause()` 停 IPC |
| 共享常量 | `electron/shared/terminal-flow-limits.ts`、`terminal-output-limits.ts` | 反压水位线与 pending/IPC 分片阈值 |
| 渲染层写入 | `src/lib/terminal-write-batcher.ts` | 串行 pump + 每 chunk ack；Tab 切换 `dropPending` 时 ack 未写数据 |
| **渲染层流控** | `src/lib/terminal-flow-control.ts` | 每 128KB 用 callback 计数；pending >10 阻塞，<5 恢复 |
| IPC | `terminal:ackData`（preload / main） | xterm 处理完一批 → 主进程 `ackActiveOutput` |
| Shell 高亮 | `src/lib/terminal-shell-addons.ts` | 100ms 节流；暂停/不可见时跳过高亮；避免 rAF cancel 抖动 |
| 拖拽暂停 | `useWindowTitleDragPause.ts`、`terminal-render-pause.ts` | 标题栏 pointerdown 即停 IPC/写入；`window-dragging` 隐藏终端层 |

**反压水位线**（`electron/shared/terminal-flow-limits.ts`，与 Tabby 对齐）：

| 常量 | 值 | 含义 |
|------|-----|------|
| `TERMINAL_FLOW_MAX_CHUNK_CHARS` | 100 KB | 主进程单次推送到渲染层 |
| `TERMINAL_FLOW_MAX_UNACKED_CHARS` | 500 KB | 未 ack 在途上限（×5 chunk） |
| `TERMINAL_FLOW_HIGH_WATERMARK` | 10 | 渲染层 pending write callback 高水位 |
| `TERMINAL_FLOW_LOW_WATERMARK` | 5 | 渲染层恢复消费低水位 |
| `TERMINAL_FLOW_BYTES_THRESHOLD` | 128 KB | 累计写入后启用 callback 追踪 |

**pause 原因叠加**：`terminal-service.ts` 用 `inactive`（Tab 休眠/Attach 非活跃）与 `flow`（闭环反压）两套原因；仅当全部清除时才 `pty.resume()` / `ssh2.stream.resume()`。

**窗口拖拽**：Windows 下 `-webkit-app-region: drag` 不一定触发 `will-move`；标题栏 `pointerdown` 通过 `window:setDragging` 同步暂停主进程 flusher 与渲染层写入（`preload` 层亦丢弃 `terminal:data`），松手后恢复并 flush 积压。

**注意**：

- pending / `pausedOutput` / gate 缓冲超限时旧输出会被丢弃（保留最新内容），以避免 OOM 与 xterm `write data discarded`。
- 闭环反压会**主动降低**当前 Tab 的显示刷新速率，换取 UI（拖拽、输入、其他 Tab）响应；切走 Tab 后仍依赖 inactive pause。
- 极端洪水仍建议调低 `terminal.scrollback`、关闭 Shell 链接/日志高亮；Wterm / Ghostty 路径下 Shell 装饰高亮不可用或已跳过。

### Ghostty（ghostty-web）

`experimental.terminalEmulator = ghostty` 时，仍由 `TerminalView` 承载，但用 [ghostty-web](https://github.com/coder/ghostty-web)（Ghostty VT WASM + xterm.js 兼容 API）替代 `@xterm/xterm` 渲染。依赖：`package.json` → `ghostty-web`；WASM 经 `src/lib/ghostty-web-loader.ts` 加载。

#### 与 Wterm / Ghostty Core 的区别

| 路径 | 配置 | 渲染组件 | VT 核心 |
|------|------|----------|---------|
| **Ghostty** | `terminalEmulator=ghostty` | `TerminalView` + ghostty-web | ghostty-web 自带 WASM |
| **Wterm** | `terminalEmulator=wterm` | `WterminalView` | `@wterm/core`（可选 `ghosttyCoreEnabled` → `@wterm/ghostty`） |
| **Xterm** | `terminalEmulator=xterm`（默认） | `TerminalView` + xterm.js | xterm.js |

三者互斥；切换后需**重启应用**并**新建终端 Tab**（标题栏 / 设置 → 实验特性均可切换）。

#### 能力与限制

| 能力 | Ghostty | 说明 |
|------|---------|------|
| DOM 渲染 | ✅ | 强制 `terminal.renderer=dom`，不可 WebGL |
| Attach-PTY | ❌ | 仅 `terminalEmulator=xterm` |
| 闭环反压 ack | ✅ | Ghostty 下 `write` callback 同步 ack（见下） |
| Unicode / 复杂脚本 | ✅ | VT 原生字素簇；**不**加载 `@xterm/addon-unicode11` |
| 内联图片 Kitty | ✅ | 见「Shell 与图片协议」 |
| SIXEL / iTerm IIP | ❌ | 不加载 `@xterm/addon-image` |
| xterm 链接/日志装饰高亮 | ❌ | 无 `registerDecoration`；链接可依赖 ghostty 内置检测 |
| SerializeAddon（Attach 快照） | ❌ | 无 `@xterm/addon-serialize` |

#### 输入与按键

- **自定义按键处理器**：ghostty-web 的 `attachCustomKeyEventHandler` 返回值语义与 xterm **相反**（`true`=阻止终端）。统一封装于 `src/lib/terminal-custom-key-handler.ts`。
- **可打印字符快速路径**：无 Ctrl/Alt/Meta 的按键跳过快捷键 Store 分支，减轻每键开销。
- **回显延迟**：字符需 PTY 往返 + Canvas rAF 绘制，通常比 xterm WebGL 慢半拍；`src/lib/terminal-sync-output.ts` 对 Ghostty 在 WASM 写入后立即 ack，避免 batcher 再等一帧。

#### Shell 与图片协议

开启 `shell.terminalInlineImages` 时：

| 模拟器 | PTY 环境变量 | 协议 | `niozy-cat` |
|--------|--------------|------|-------------|
| xterm | `NIOZY_IMAGE_PROTOCOL=iip` | SIXEL + iTerm IIP（`@xterm/addon-image`） | IIP |
| ghostty | `NIOZY_IMAGE_PROTOCOL=kitty` | Kitty 图形（VT 原生） | Kitty 分块 |

主进程在 `terminal:create` 注入 `NIOZY_TERMINAL_EMULATOR`、`NIOZY_IMAGE_PROTOCOL`（`electron/shell-integration.ts`、`electron/main/index.ts`）。

Shell 集成（`electron/scripts/shell-integration.ps1`）：

- 始终发送 **OSC 7** 报告 Cwd（ST 终止，`terminal-cwd-parser.ts` 可解析）。
- **`NIOZY_TERMINAL_EMULATOR=ghostty` 时不发送 OSC 633**，避免 ghostty VT 报 `invalid OSC command` 警告。

详见 [功能增强SHELL.md](./功能增强SHELL.md)。

#### 核心模块

| 模块 | 作用 |
|------|------|
| `src/lib/ghostty-web-loader.ts` | 共享 Ghostty WASM、`loadTerminalModules()` |
| `src/lib/terminal-custom-key-handler.ts` | 快捷键 + ghostty/xterm 语义适配 |
| `src/lib/terminal-emulator.ts` | `isGhosttyEmulator`、`isDomOnlyTerminalEmulator` 等 |
| `src/lib/terminal-shell-addons.ts` | Ghostty 下跳过 xterm 专用 addon |
| `src/lib/terminal-sync-output.ts` | Ghostty 快速 ack 写入 |
| `src/components/settings/ExperimentalSettings.tsx` | 三档模拟器选择 |
| `src/components/layout/TitleBarTerminalControls.tsx` | 标题栏引擎切换 |

### Wterm 与 Ghostty Core

`terminalEmulator=wterm` 时使用 `WterminalView`。`ghosttyCoreEnabled=true` 时在 Wterm 路径启用 `@wterm/ghostty` WASM 核心（与上节 **ghostty-web 路径无关**）。

### 引擎切换提示

`TitleBarTerminalControls.setEmulator` — 切换后提示重启应用（`src/components/layout/TitleBarTerminalControls.tsx`）。
